        var p = 20;
         var q = 30;
         var res = subtraction(x,y);  // x,y are the arguments.
        document.write("sub is : "+res+"<br>")
         function subtraction(a,b)       // a and b are the parameters.
         {
            var sub = b - a;
             return sub;
         }